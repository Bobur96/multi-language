import { defineMessages } from "react-intl";

export default defineMessages({
  quote: {
    title: "Default Title",
    change: "Change language",
    nav1: "Default nav, Check language files.",
    nav2: "Default nav, Check language files.",
    nav3: "Default nav, Check language files.",
    nav4: "Default nav, Check language files.",
  },
});
